#include<iostream>
#include<math.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    int original=n;
    int sum=0;
    while(n>0){
        
        int ld=n%10;
        sum+=pow(ld,3);
        n=n/10;
    }
    if(original==sum){
        cout<<"given num is armstrong number";
    }
    else{
        cout<<"given num is not armstrong number";
    }
    return 0;
}
