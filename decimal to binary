#include<iostream>
using namespace std;
int decimaltobinary(int n){
    int x=1;
    int ans=0;
    while(x<=n)
    x*=2;
    x/=2;
    while(x>0){
        int ld=n/x;
        n=n-ld*x;
        x/=2;
        ans=ans*10+ld;
    }
    return ans;

}
int main(){
    int n;
    cin>>n;
    cout<<decimaltobinary(n)<<endl;
}
